============
Installation
============

At the command line::

    $ easy_install Flask-Stats

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv Flask-Stats
    $ pip install Flask-Stats