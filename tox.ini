[tox]
envlist = py26, py27, py33

[testenv]
setenv =
    PYTHONPATH = {toxinidir}:{toxinidir}/Flask-Stats
commands = nosetests
deps =
    -r{toxinidir}/requirements.txt
    -r{toxinidir}/requirements-testing.txt
